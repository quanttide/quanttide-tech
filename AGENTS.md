# AGENTS.md

## SKILL 快速索引

| 场景 | SKILL 位置 | 触发时机 |
|------|-----------|----------|
| 提交代码 | `.agents/skills/devops-commit/SKILL.md` | 用户请求提交代码时（含子模块提交） |
| 发布版本 | `.agents/skills/devops-release/SKILL.md` | 用户请求发布新版本、打标签时 |
| 流程审查 | `.agents/skills/devops-review/SKILL.md` | 发布前审查、代码审查、文档审查时 |
| 文档格式 | `.agents/skills/docs-format/SKILL.md` | 操作 Markdown 文档时 |
| 子模块操作 | `.agents/skills/devops-submodule/SKILL.md` | 操作 Git 子模块（更新、提交子模块）时 |

## 契约文件

| 文件 | 用途 |
|------|------|
| `.quanttide/asset/contract.yaml` | 数字资产契约 |
| `.quanttide/docs/contract.yaml` | 文档工程契约 |

AI Agent 进入仓库后，先读取 `.quanttide/` 目录获取上下文。

## 特殊文件

| 文件 | 用途 |
|------|------|
| `AGENTS.md` | AI Agent 工作指南（本文档） |
| `CONTRIBUTING.md` | 贡献指南和 SKILL 维护 |
| `README.md` | 项目说明 |
| `ROADMAP.md` | 产品路线图 |
| `CHANGELOG.md` | 版本变更记录 |

## 元认知规则

> 元认知不是「做完再检查」的清单，而是一个**持续内观**的过程：工作同时监视自己的思维状态——在猜还是在知道、在逼近还是在绕圈、在满足用户意图还是在满足自己的臆想。

### 模式觉察

区分三种工作模式，不跳阶段：

- **理解模式**：听清用户说什么、不说什么。用户给出了具体清单就不自作主张扩展。
- **执行模式**：选对的工具、对的参数、对的路径。不确定时先查文档而非试错。
- **验证模式**：构建产物是否可访问？链接是否真实可达？不把"没报错"当成"成功"。

### 置信度校准

每次操作前自问：这件事我是**知道**正确的做法，还是在**猜**？猜的东西去文档验证，不往仓库里推。

### 收敛监控

同一问题反复尝试时，检查是**在逼近目标**还是**在绕圈**。绕圈意味着：理解有误、工具用错、或意图已漂移。

### 意图对齐

回答和操作只服务于用户陈述的意图。每轮回复前核对：用户要的是什么？我做的这些是在满足它，还是在做我叫他做的东西？如果发现后者，立即停并确认。
